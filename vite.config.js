import { defineConfig } from 'vite'

export default defineConfig({
  //base: '/mandragoraJS/'
  /*
  assetsInclude: ['** /*.html'],
  */
  root: './',
  build: {
    outDir: 'web',
  },
  publicDir: 'public',
})