import Html from './index.html?raw';
//import { getData, createData, putData, deleteData, getDataById } from "../../services/firebase.js";

export function dashboard() {
    
    const onLoad = () => {
       
    }

    setTimeout(onLoad, 0);
    return Html;
}